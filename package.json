{
  "name": "pitchmarket",
  "version": "0.1.0",
  "private": true,
  "description": "Verifiable World Cup prediction markets on Solana (TxODDS hackathon)",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "anchor:build": "anchor build",
    "anchor:test": "anchor test",
    "relayer:dev": "pnpm --filter @pitchmarket/relayer dev",
    "app:dev": "pnpm --filter @pitchmarket/app dev"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "prettier": "^3.4.2",
    "typescript": "^5.7.3"
  },
  "packageManager": "pnpm@9.12.3"
}
